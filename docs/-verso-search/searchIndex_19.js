window.docContents[19].resolve({"/Article-IV___-Universal-mapping-properties/#A-Lean-Companion-to-Conceptual-Mathematics--Article-IV___-Universal-mapping-properties--3___-Initial-object":{"contents":"S is an initial object of 𝒞 if for every object X of 𝒞 there is exactly one 𝒞-map {S \\rightarrow X}.\n\nThe relevant mathlib definitions here are HasInitial and IsInitial, which we print below for reference (cf. HasTerminal and IsTerminal above).\n\n#print HasInitial\n\n\n@[reducible] def CategoryTheory.Limits.HasInitial.{v₁, u₁} : (C : Type u₁) → [Category.{v₁, u₁} C] → Prop :=\nfun C [Category.{v₁, u₁} C] ↦ HasColimitsOfShape (Discrete PEmpty.{1}) C\n\n\n#print IsInitial\n\n\n@[reducible] def CategoryTheory.Limits.IsInitial.{v₁, u₁} : {C : Type u₁} →\n  [Category.{v₁, u₁} C] → C → Type (max (max 0 u₁) v₁) :=\nfun {C} [Category.{v₁, u₁} C] X ↦ IsColimit (asEmptyCocone X)\n\n\nIf S_1, S_2 are both initial in 𝒞 then the (unique) map {S_1 \\rightarrow S_2} is an isomorphism.\n\ncf. the proof of the earlier proposition on uniqueness of terminal objects (pp. 213–214).example {𝒞 : Type u} [Category.{v, u} 𝒞] {S₁ S₂ : 𝒞}\n    (hS₁ : IsInitial S₁) (hS₂ : IsInitial S₂)\n    : ∃! s : S₁ ⟶ S₂, IsIso s := by\n  let f : S₁ ⟶ S₂ := hS₁.to S₂\n  let g : S₂ ⟶ S₁ := hS₂.to S₁\n  have h_id₁ : g ⊚ f = 𝟙 S₁ := hS₁.hom_ext _ _\n  have h_id₂ : f ⊚ g = 𝟙 S₂ := hS₂.hom_ext _ _\n  use f\n  constructor\n  · exact { out := ⟨g, h_id₁, h_id₂⟩ }\n  · intros\n    exact hS₁.hom_ext _ _\n\n\nIn each of 𝑺, 𝑺⇊, and 𝑺↻, if \\mathbf{0} is an initial object and {X \\xrightarrow{f} \\mathbf{0}} is a map, then both(a) for any {X \\xrightarrow{g} \\mathbf{0}}, {g = f}; and(b) X itself is initial.\n\nFor each of 𝑺, 𝑺⇊, and 𝑺↻, we follow the same strategy. To prove part (a) we show that f and g are both 'empty' maps and therefore trivially equal; to prove part (b) we show that for every object Y of the respective category there is exactly one morphism {X \\rightarrow Y}, namely the empty map.In the case of 𝑺 we haveexample\n    {Zero X : Type}\n    (hZero : IsInitial Zero) (f : X ⟶ Zero)\n    : (∀ g : X ⟶ Zero, g = f) ∧ Nonempty (IsInitial X) := by\n  have e : Zero ⟶ Empty := hZero.to Empty\n  constructor\n  · intro g\n    exact IsInitial.strict_hom_ext hZero g f\n  · constructor\n    apply IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := hZero.to Y ⊚ f\n        uniq := by\n          intros\n          funext x\n          exact (e (f x)).elim\n      }\n    )\nIn the case of 𝑺⇊ we haveexample\n    {Zero X : IrreflexiveGraph}\n    (hZero : IsInitial Zero) (f : X ⟶ Zero)\n    : (∀ g : X ⟶ Zero, g = f) ∧ Nonempty (IsInitial X) := by\n  let emptyIG : IrreflexiveGraph := {\n    carrierA := Empty\n    carrierD := Empty\n    toSrc := Empty.elim\n    toTgt := Empty.elim\n  }\n  have e : Zero ⟶ emptyIG := hZero.to emptyIG\n  constructor\n  · intros\n    ext x\n    · exact (e.val.1 (f.val.1 x)).elim\n    · exact (e.val.2 (f.val.2 x)).elim\n  · constructor\n    apply IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := hZero.to Y ⊚ f\n        uniq := by\n          intros\n          ext x\n          · exact (e.val.1 (f.val.1 x)).elim\n          · exact (e.val.2 (f.val.2 x)).elim\n      }\n    )\nIn the case of 𝑺↻ we haveexample\n    {Zero X : SetWithEndomap}\n    (hZero : IsInitial Zero) (f : X ⟶ Zero)\n    : (∀ g : X ⟶ Zero, g = f) ∧ Nonempty (IsInitial X) := by\n  let emptySWE : SetWithEndomap := {\n    carrier := Empty\n    toEnd := Empty.elim\n  }\n  have e : Zero ⟶ emptySWE := hZero.to emptySWE\n  constructor\n  · intros\n    apply Subtype.ext\n    funext x\n    exact (e.val (f.val x)).elim\n  · constructor\n    apply IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := hZero.to Y ⊚ f\n        uniq := by\n          intros\n          apply Subtype.ext\n          funext x\n          exact (e.val (f.val x)).elim\n      }\n    )\n\n\nDefine the category \\mathbf{1}/𝑺 of pointed sets: an object is a map {\\mathbf{1} \\xrightarrow{x_0} X} in 𝑺, and a map from {\\mathbf{1} \\xrightarrow{x_0} X} to {\\mathbf{1} \\xrightarrow{y_0} Y} is a map {X \\xrightarrow{f} Y} in 𝑺 for whichstructure PointedSet where\n  carrier : Type\n  point : One ⟶ carrier\n\ninstance : Category PointedSet where\n  Hom X Y := {\n    f : X.carrier ⟶ Y.carrier //\n        f ⊚ X.point = Y.point -- commutes\n  }\n  id X := ⟨𝟙 X.carrier, rfl⟩\n  comp f g := ⟨\n    g.val ⊚ f.val,\n    by\n      obtain hf_comm := f.property\n      obtain hg_comm := g.property\n      rw [← Category.assoc, hf_comm, hg_comm]\n  ⟩\nShow that in \\mathbf{1}/𝑺 any terminal object is also initial and that part (b) of the previous exercise is false.\n\nThe terminal object in \\mathbf{1}/𝑺 is the identity map on \\mathbf{1}.example\n    {T : PointedSet}\n    (hT : IsTerminal T)\n    : Nonempty (IsInitial T)\n    ∧ ¬(∀ X : PointedSet, Nonempty (IsInitial X)) := by\n  constructor\n  · -- Define the object PSOne,\n    let PSOne : PointedSet := {\n      carrier := One\n      point := 𝟙 One\n    }\n    -- and show that PSOne is terminal\n    have hT' : IsTerminal PSOne := by\n      apply IsTerminal.ofUnique (\n        h := fun X ↦ {\n          default := by\n            exact ⟨fun _ ↦ (), rfl⟩\n          uniq := by\n            intros\n            rfl\n        }\n      )\n    -- PSOne is isomorphic to T,\n    have h_iso : PSOne ≅ T := IsTerminal.uniqueUpToIso hT' hT\n    -- so it suffices to show that PSOne is initial\n    constructor\n    apply IsInitial.ofIso _ h_iso\n    exact IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := by\n          exact ⟨Y.point, rfl⟩\n        uniq := by\n          intro a\n          exact Subtype.ext a.property\n      }\n    )\n  · -- Consider the pointed set X with two elements\n    let X : PointedSet := {\n      carrier := Fin 2\n      point := fun _ ↦ 0\n    }\n    -- and two distinct endomorphisms\n    let f₁ : X ⟶ X := {\n      val := fun\n        | 0 => 0\n        | 1 => 0\n      property := rfl\n    }\n    let f₂ : X ⟶ X := {\n      val := fun\n        | 0 => 0\n        | 1 => 1\n      property := rfl\n    }\n    intro h\n    -- If X is intial,\n    obtain ⟨hI⟩ := h X\n    -- then there is a unique morphism from X to itself, so f₁ = f₂\n    have h_eq : f₁ = f₂ := hI.hom_ext f₁ f₂\n    -- But f₁ and f₂ are distinct, so X is not initial\n    have : f₁.val = f₂.val := congr_arg Subtype.val h_eq\n    exact zero_ne_one (congr_fun this 1)\nWe can faithfully implement the same proof using mathlib's Under category, as demonstrated below.abbrev PointedSet := Under Unit\n\nexample\n    {T : PointedSet}\n    (hT : IsTerminal T)\n    : Nonempty (IsInitial T)\n    ∧ ¬(∀ X : PointedSet, Nonempty (IsInitial X)) := by\n  constructor\n  · -- Define the object PSOne,\n    let PSOne : PointedSet := Under.mk (𝟙 Unit)\n    -- and show that PSOne is terminal\n    have hT' : IsTerminal PSOne := by\n      apply IsTerminal.ofUnique (\n        h := fun X ↦ {\n          default := Under.homMk (fun _ ↦ ())\n          uniq := by\n            intros\n            rfl\n        }\n      )\n    -- PSOne is isomorphic to T,\n    have h_iso : PSOne ≅ T := IsTerminal.uniqueUpToIso hT' hT\n    -- so it suffices to show that PSOne is initial\n    constructor\n    apply IsInitial.ofIso _ h_iso\n    exact IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := Under.homMk Y.hom\n        uniq := by\n          intro a\n          exact Under.UnderMorphism.ext (Under.w a)\n      }\n    )\n  · -- Consider the pointed set X with two elements\n    let X : PointedSet := Under.mk (fun _ ↦ (0 : Fin 2))\n    -- and two distinct endomorphisms\n    let f₁ : X ⟶ X := Under.homMk (fun\n      | 0 => 0\n      | 1 => 0\n      : Fin 2 ⟶ Fin 2)\n    let f₂ : X ⟶ X := Under.homMk (fun\n      | 0 => 0\n      | 1 => 1\n      : Fin 2 ⟶ Fin 2)\n    intro h\n    -- If X is intial,\n    obtain ⟨hI⟩ := h X\n    -- then there is a unique morphism from X to itself, so f₁ = f₂\n    have h_eq : f₁ = f₂ := hI.hom_ext f₁ f₂\n    -- But f₁ and f₂ are distinct, so X is not initial\n    have : f₁.right = f₂.right := Under.UnderMorphism.ext_iff.mp h_eq\n    exact Fin.zero_ne_one (congr_fun this (1 : Fin 2))\nA more idiomatic Lean proof would likely use mathlib's Pointed category, but at the cost of moving away from the book's morphism-based definition of a point. A version of the same proof using Pointed is given below.example\n    {T : Pointed.{0}}\n    (hT : IsTerminal T)\n    : Nonempty (IsInitial T)\n    ∧ ¬(∀ X : Pointed.{0}, Nonempty (IsInitial X)) := by\n  constructor\n  · -- Define the object PSOne,\n    let PSOne : Pointed := {\n      X := One\n      point := ()\n    }\n    -- and show that PSOne is terminal\n    have hT' : IsTerminal PSOne := by\n      apply IsTerminal.ofUnique (\n        h := fun X ↦ {\n          default := by\n            exact ⟨fun _ ↦ (), rfl⟩\n          uniq := by\n            intros\n            rfl\n        }\n      )\n    -- PSOne is isomorphic to T,\n    have h_iso : PSOne ≅ T := IsTerminal.uniqueUpToIso hT' hT\n    -- so it suffices to show that PSOne is initial\n    constructor\n    apply IsInitial.ofIso _ h_iso\n    exact IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := by\n          exact ⟨fun _ ↦ Y.point, rfl⟩\n        uniq := by\n          intro a\n          apply Pointed.Hom.ext\n          funext x\n          exact a.map_point\n      }\n    )\n  · -- Consider the pointed set X with two elements\n    let X : Pointed := {\n      X := Fin 2\n      point := 0\n    }\n    -- and two distinct endomorphisms\n    let f₁ : X ⟶ X := {\n      toFun := fun\n        | 0 => 0\n        | 1 => 0\n      map_point := rfl\n    }\n    let f₂ : X ⟶ X := {\n      toFun := fun\n        | 0 => 0\n        | 1 => 1\n      map_point := rfl\n    }\n    intro h\n    -- If X is intial,\n    have := h X\n    obtain ⟨hI⟩ := h X\n    -- then there is a unique morphism from X to itself, so f₁ = f₂\n    have h_eq : f₁ = f₂ := hI.hom_ext f₁ f₂\n    -- But f₁ and f₂ are distinct, so X is not initial\n    have : f₁.toFun = f₂.toFun := Pointed.Hom.ext_iff.mp h_eq\n    exact zero_ne_one (congr_fun this 1)\n\n\nLet \\mathbf{2} be a fixed 2-point set.abbrev Two := Fin 2\nDefine the category \\mathbf{2}/𝑺 of bipointed sets to have as objects the 𝑺-maps {\\mathbf{2} \\xrightarrow{\\bar{x}} X} and as maps the 𝑺-maps satisfying {f\\bar{x} = \\bar{y}}structure BipointedSet where\n  carrier : Type\n  point : Two ⟶ carrier\n\ninstance : Category BipointedSet where\n  Hom X Y := {\n    f : X.carrier ⟶ Y.carrier //\n        f ⊚ X.point = Y.point -- commutes\n  }\n  id X := ⟨𝟙 X.carrier, rfl⟩\n  comp f g := ⟨\n    g.val ⊚ f.val,\n    by\n      obtain hf_comm := f.property\n      obtain hg_comm := g.property\n      rw [← Category.assoc, hf_comm, hg_comm]\n  ⟩\nShow that in \\mathbf{2}/𝑺 'the' initial object is the identity map {\\mathbf{2} \\rightarrow \\mathbf{2}} and that part (a) of Exercise 8 is false, i.e. an object can have more than one map to the initial object.\n\nIn a similar fashion to Exercise 9, we provide three different implementations of the same proof.The first version of the proof uses our custom BipointedSet category.def BSTwo : BipointedSet := {\n  carrier := Two\n  point := 𝟙 Two\n}\n\nexample : Nonempty (IsInitial BSTwo)\n          ∧ ¬(∀ (X : BipointedSet) (f g : X ⟶ BSTwo), f = g) := by\n  constructor\n  · -- Show that BSTwo is initial\n    constructor\n    apply IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := by\n          exact ⟨Y.point, rfl⟩\n        uniq := by\n          intro a\n          exact Subtype.ext a.property\n      }\n    )\n  · -- Consider the bipointed set X with three elements\n    let X : BipointedSet := {\n      carrier := Fin 3\n      point := fun\n        | 0 => 0\n        | 1 => 1\n    }\n    -- and two distinct morphisms to the initial object\n    let f : X ⟶ BSTwo := {\n      val := (fun\n      | 0 => 0\n      | 1 => 1\n      | 2 => 0\n      : X.carrier ⟶ Two)\n      property := by\n        funext x\n        fin_cases x <;> rfl\n    }\n    let g : X ⟶ BSTwo := {\n      val := (fun\n      | 0 => 0\n      | 1 => 1\n      | 2 => 1\n      : X.carrier ⟶ Two)\n      property := by\n        funext x\n        fin_cases x <;> rfl\n    }\n    intro h\n    have : f.val = g.val := congr_arg Subtype.val (h X f g)\n    exact Fin.zero_ne_one (congr_fun this 2)\nThe second version of the proof uses mathlib's Under category.abbrev BipointedSet := Under (Fin 2)\n\ndef BSTwo : BipointedSet := Under.mk (𝟙 (Fin 2))\n\nexample : Nonempty (IsInitial BSTwo)\n          ∧ ¬(∀ (X : BipointedSet) (f g : X ⟶ BSTwo), f = g) := by\n  constructor\n  · -- Show that BSTwo is initial\n    constructor\n    apply IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := Under.homMk Y.hom\n        uniq := by\n          intro a\n          exact Under.UnderMorphism.ext (Under.w a)\n      }\n    )\n  · -- Consider the bipointed set X with three elements\n    let X : BipointedSet := Under.mk (fun\n      | 0 => 0\n      | 1 => 1\n      : Fin 2 ⟶ Fin 3)\n    -- and two distinct morphisms to the initial object\n    let f : X ⟶ BSTwo := Under.homMk (fun\n      | 0 => 0\n      | 1 => 1\n      | 2 => 0\n      : Fin 3 ⟶ Fin 2)\n      (by\n        funext (x : Fin 2)\n        fin_cases x <;> rfl)\n    let g : X ⟶ BSTwo := Under.homMk (fun\n      | 0 => 0\n      | 1 => 1\n      | 2 => 1\n      : Fin 3 ⟶ Fin 2)\n      (by\n        funext (x : Fin 2)\n        fin_cases x <;> rfl)\n    intro h\n    have : f.right = g.right :=\n        Under.UnderMorphism.ext_iff.mp (h X f g)\n    exact Fin.zero_ne_one (congr_fun this (2 : Fin 3))\nThe third version of the proof uses mathlib's Bipointed category.abbrev BSTwo : Bipointed := ⟨Fin 2, (0, 1)⟩\n\nexample : Nonempty (IsInitial BSTwo)\n          ∧ ¬(∀ (X : Bipointed) (f g : X ⟶ BSTwo), f = g) := by\n  constructor\n  · -- Show that BSTwo is initial\n    constructor\n    apply IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := Bipointed.Hom.mk (fun\n          | 0 => Y.toProd.1\n          | 1 => Y.toProd.2\n          : Fin 2 ⟶ Y.X)\n          rfl\n          rfl\n        uniq := by\n          intro a\n          apply Bipointed.Hom.ext\n          funext x\n          fin_cases x\n          · exact a.map_fst\n          · exact a.map_snd\n      }\n    )\n  · -- Consider the bipointed set X with three elements\n    let X : Bipointed := ⟨Fin 3, (0, 1)⟩\n    -- and two distinct morphisms to the initial object\n    let f : X ⟶ BSTwo := Bipointed.Hom.mk (fun\n      | 0 => 0\n      | 1 => 1\n      | 2 => 0\n      : Fin 3 ⟶ Fin 2)\n      rfl\n      rfl\n    let g : X ⟶ BSTwo := Bipointed.Hom.mk (fun\n      | 0 => 0\n      | 1 => 1\n      | 2 => 1\n      : Fin 3 ⟶ Fin 2)\n      rfl\n      rfl\n    intro h\n    have : f.toFun = g.toFun := Bipointed.Hom.ext_iff.mp (h X f g)\n    exact Fin.zero_ne_one (congr_fun this (2 : Fin 3))\n\n\nShow that in the category 𝑺, if an object X is not an initial object, then X has at least one point (map from a terminal object). Show that the same statement is false in both the categories 𝑺↻ and 𝑺⇊.\n\nIn our answer below to the first part of the exercise, we use the category Type instead of the category 𝑺.example : ∀ X, IsEmpty (IsInitial X) → Nonempty (⊤_ Type ⟶ X) := by\n  intro X hX\n  have h_nonempty : Nonempty X := by\n    by_contra h_empty\n    apply hX.false\n    exact IsInitial.ofUnique (\n      h := fun Y ↦ {\n        default := fun x ↦ False.elim (h_empty (Nonempty.intro x))\n        uniq := by\n          intros\n          funext x\n          exact False.elim (h_empty (Nonempty.intro x))\n      }\n    )\n  exact Nonempty.map (fun x _ ↦ x) h_nonempty\nTo work with the same statement in 𝑺↻, we first need a HasTerminal instance for the category SetWithEndomap, since the statement includes the terminal object ⊤_.instance : HasTerminal SetWithEndomap where\n  has_limit _ := HasLimit.mk' (\n    exists_limit := Nonempty.intro {\n      cone := {\n        pt := {\n          carrier := Unit\n          toEnd := id\n        }\n        π := { app := fun j ↦ j.as.elim }\n      }\n      isLimit := {\n        lift := fun _ ↦ {\n          val := fun _ ↦ ()\n          property := rfl\n        }\n        fac := fun _ j ↦ j.as.elim\n        uniq _ _ _ := rfl\n      }\n    }\n  )\nWe can now show that the statement is false in the category 𝑺↻.example : ¬(∀ X : SetWithEndomap, IsEmpty (IsInitial X) →\n    Nonempty (⊤_ SetWithEndomap ⟶ X)) := by\n  push_neg\n  -- Define an object X which is not initial\n  -- and has no morphism from the terminal object\n  let X : SetWithEndomap := {\n    carrier := Fin 2\n    toEnd := fun\n      | 0 => 1\n      | 1 => 0\n  }\n  use X\n  constructor\n  · -- Assume towards a contradiction that X is initial\n    refine ⟨fun hX : IsInitial X ↦ ?_⟩\n    -- Define an object emptySWE (which is in fact initial)\n    let emptySWE : SetWithEndomap := {\n      carrier := Empty\n      toEnd := Empty.elim\n    }\n    -- Since, by assumption, X is initial, there exists X ⟶ emptySWE\n    let f : X ⟶ emptySWE := hX.to emptySWE\n    -- But the assumption is false, since the carrier of emptySWE is ∅\n    exact (f.val 0).elim\n  · -- Assume towards a contradiction that there exists ⊤_ ⟶ X\n    refine ⟨fun hX : ⊤_ SetWithEndomap ⟶ X ↦ ?_⟩\n    -- Define a probe object U\n    let U : SetWithEndomap := {\n      carrier := Unit\n      toEnd := id\n    }\n    let f : U ⟶ X := hX ⊚ terminal.from U\n    -- Perform case analysis to show that the assumption is false\n    have h_comm : (f.val ⊚ U.toEnd) () = (X.toEnd ⊚ f.val) () :=\n        congr_fun f.property ()\n    dsimp [U, X] at h_comm\n    generalize f.val () = x at h_comm\n    fin_cases x <;> trivial\nOur proof that the same statement is false in 𝑺⇊ is essentially identical to our proof for 𝑺↻, just with the endomaps of sets translated into irreflexive graphs.instance : HasTerminal IrreflexiveGraph where\n  has_limit _ := HasLimit.mk' (\n    exists_limit := Nonempty.intro {\n      cone := {\n        pt := {\n          carrierA := Unit\n          carrierD := Unit\n          toSrc := fun _ ↦ ()\n          toTgt := fun _ ↦ ()\n        }\n        π := { app := fun j ↦ j.as.elim }\n      }\n      isLimit := {\n        lift := fun _ ↦ {\n          val := (fun _ ↦ (), fun _ ↦ ())\n          property := by\n            constructor <;> rfl\n        }\n        fac := fun _ j ↦ j.as.elim\n        uniq _ _ _ := rfl\n      }\n    }\n  )\n\nexample : ¬(∀ X : IrreflexiveGraph, IsEmpty (IsInitial X) →\n    Nonempty (⊤_ IrreflexiveGraph ⟶ X)) := by\n  push_neg\n  -- Define an object X which is not initial\n  -- and has no morphism from the terminal object\n  let X : IrreflexiveGraph := {\n    carrierA := Fin 2\n    carrierD := Fin 2\n    toSrc := fun\n      | 0 => 0\n      | 1 => 1\n    toTgt := fun\n      | 0 => 1\n      | 1 => 0\n  }\n  use X\n  constructor\n  · -- Assume towards a contradiction that X is initial\n    refine ⟨fun hX : IsInitial X ↦ ?_⟩\n    -- Define an object emptyIG (which is in fact initial)\n    let emptyIG : IrreflexiveGraph := {\n      carrierA := Empty\n      carrierD := Empty\n      toSrc := Empty.elim\n      toTgt := Empty.elim\n    }\n    -- Since, by assumption, X is initial, there exists X ⟶ emptyIG\n    let f : X ⟶ emptyIG := hX.to emptyIG\n    -- But the assumption is false, since carrierA of emptyIG is ∅\n    exact (f.val.1 0).elim\n  · -- Assume towards a contradiction that there exists ⊤_ ⟶ X\n    refine ⟨fun hX : ⊤_ IrreflexiveGraph ⟶ X ↦ ?_⟩\n    -- Define a probe object U\n    let U : IrreflexiveGraph := {\n      carrierA := Unit\n      carrierD := Unit\n      toSrc := fun _ ↦ ()\n      toTgt := fun _ ↦ ()\n    }\n    let f : U ⟶ X := hX ⊚ terminal.from U\n    -- Perform case analysis to show that the assumption is false\n    have h_src_comm := congr_fun f.property.1 ()\n    have h_tgt_comm := congr_fun f.property.2 ()\n    dsimp [U, X] at h_src_comm h_tgt_comm\n    generalize f.val.1 () = x₁ at h_src_comm h_tgt_comm\n    generalize f.val.2 () = x₂ at h_src_comm h_tgt_comm\n    fin_cases x₁ <;> fin_cases x₂ <;> trivial\n\n\n","context":"A Lean Companion to Conceptual Mathematics\u0009Article IV: Universal mapping properties","header":"3. Initial object","id":"/Article-IV___-Universal-mapping-properties/#A-Lean-Companion-to-Conceptual-Mathematics--Article-IV___-Universal-mapping-properties--3___-Initial-object"}});